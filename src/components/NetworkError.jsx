import React from 'react';

function NetworkError() {
	return (
		<div>The system can not find a suitable network. Install MetaMask and select Ropsten Network <span role="img" aria-label="robot">🤖</span></div>
	);
}

export default NetworkError;
